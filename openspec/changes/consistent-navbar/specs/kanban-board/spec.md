## MODIFIED Requirements

### Requirement: 공통 NavBar 헤더 적용
칸반 화면은 공통 NavBar 컴포넌트를 사용하며 activePage='kanban'으로 렌더링한다.

#### Scenario: 칸반 탭 활성 표시
- **WHEN** 칸반 화면에서 헤더를 보면
- **THEN** "칸반" 탭에 하단 흰색 밑줄이 표시되고 다른 탭은 동일 크기·위치를 유지한다
